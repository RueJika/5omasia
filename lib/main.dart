import 'package:flutter/material.dart';

void main() {
  runApp(const Khomasia());
}

class Khomasia extends StatelessWidget {
  const Khomasia({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}
